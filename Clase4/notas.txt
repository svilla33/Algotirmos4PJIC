Tail: se usa para ahorrar memoria se añaden variables, 
para python no suele sevir mucho

Memorizacion: te ayuda a no repetir calculos
    cuando hay problemas recursivos usamos diccionarios
    diccinario: clave -> valor

    hash: tecnica de busqueda(eso dijo el profe)

fidobacci con diccionario y recursividad

F(n):
    if n <= 1:
        return n
    return F(n-1) + F(n-2
    
    
pasos para converitir a diccionario:
1. agregar un parametro nuevo

F(n, memo={}) #los diccionarios usan {} en vez de [] como los vectores
---------------
F(n, memo{}):
    if n <= 1:
        return n
    if n in memo:
        return memo[n]
    memo[n]= F(n-1) + F(n-2)
    return memo[n]

busqueda binaria(lo tengo pendiente)

consultar Algoritmo SHUTING Yard (Dijkstra, 1961)